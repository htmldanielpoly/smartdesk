# Local-dev config for running forum-service outside Docker.
MONGO_URI=mongodb://localhost:27017
MONGO_DB=smartdesk_forum
# Must match the api-service secret: the forum service validates the same JWTs.
JWT_SECRET=change-me-in-prod
