# Local-dev config for running ai-service outside Docker.
# Set LOCAL_AI_ENABLED=false to skip the local LLM entirely and run with
# rule-based fallbacks (no llama-cpp-python install, no model download).
LOCAL_AI_ENABLED=true
MODELS_DIR=models
AUTO_DOWNLOAD_MODELS=true
